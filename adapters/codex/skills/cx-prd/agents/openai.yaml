interface:
  display_name: "CX PRD"
  short_description: "创建需求文档并注册 feature"
  default_prompt: "Follow the shared cx workflow PRD protocol, deterministically scaffold the feature, collect requirements, write the PRD, and register the feature in the shared cx core."
